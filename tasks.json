{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build",
        "type": "shell",
        "command": "g++",
        "args": [
          "-std=c++17",
          "-I${workspaceFolder}/include",
          "-o",
          "${workspaceFolder}/program.exe",
          "${workspaceFolder}/src/*.cpp",
          "${workspaceFolder}/obj/*.o"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "presentation": {
          "reveal": "silent",
          "panel": "new"
        }
      }
    ]
  }
  